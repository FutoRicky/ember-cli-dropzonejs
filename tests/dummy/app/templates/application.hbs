<h5>This is only a demo and it will not upload any files. There is no specified
  url.</h5>
<DropZone
  @url="#"
  @addRemoveLinks={{true}}
  @autoProcessQueue={{false}}
  @addedfile={{this.addedfile}}
/>